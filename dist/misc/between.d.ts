/**
 * Check if value (x) is between two numbers
 * @param x
 * @param min
 * @param max
 * @param inclusive
 * @returns
 */
declare function between(x: number, min: number, max: number, inclusive?: boolean): boolean;
export { between };
